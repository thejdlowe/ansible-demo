- name: Create folder
  file:
    path: /etc/myapp
    state: directory
    mode: "0755"
  tags: create_folder
- name: Copy JSON using jinja2
  include_tasks: build_json.yml
- name: install packages
  apt:
    name: "{{ item }}"
    state: latest
  loop:
    - jq
  tags: install_packages
